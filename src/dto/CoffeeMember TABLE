CREATE TABLE COFFEE_MEMBER(
	ID VARCHAR2(30) PRIMARY KEY,
	PWD VARCHAR2(30) NOT NULL,
	NAME VARCHAR2(50) NOT NULL,
	EMAIL VARCHAR2(50) NOT NULL
);

insert into COFFEE_MEMBER
values('admin', 'admin' , '관리자' , 'admin@gmail.com');

SELECT *
FROM COFFEE_MEMBER;



